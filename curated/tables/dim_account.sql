CREATE TABLE IF NOT EXISTS RISK_DB.CURATED.DIM_ACCOUNT (
    DIM_ACCOUNT_SK NUMBER DEFAULT RISK_DB.CURATED.SEQ_DIM_ACCOUNT.NEXTVAL,
    ACCOUNT_ID TEXT,
    CUSTOMER_ID TEXT,
    ACCOUNT_TYPE TEXT,
    CURRENCY TEXT,
    OPENING_DATE DATE,
    STATUS TEXT,
    BRANCH_CODE TEXT,
    COUNTRY TEXT,
    BALANCE NUMBER,
    CREDIT_LIMIT NUMBER,
    OVERDRAFT_LIMIT NUMBER,
    LAST_ACTIVITY_DATE DATE,
    RISK_SCORE NUMBER,
    DAYS_SINCE_LAST_ACTIVITY NUMBER,
    IS_DORMANT BOOLEAN,
    IS_OVERDRAWN BOOLEAN,
    EFFECTIVE_FROM TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP(),
    EFFECTIVE_TO TIMESTAMP_NTZ DEFAULT '9999-12-31'::TIMESTAMP_NTZ,
    IS_CURRENT BOOLEAN DEFAULT TRUE,
    RECORD_HASH TEXT
);
