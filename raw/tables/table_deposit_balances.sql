CREATE TABLE IF NOT EXISTS RISK_DB.RAW.DEPOSIT_BALANCES (
  DEPOSIT_ID STRING,
  ACCOUNT_ID STRING,
  CUSTOMER_ID STRING,
  DEPOSIT_TYPE STRING,
  BALANCE NUMBER(18,2),
  CURRENCY STRING,
  EFFECTIVE_DATE DATE,
  MATURITY_DATE STRING,
  INTEREST_RATE NUMBER(6,4),
  INSURED_AMOUNT NUMBER(18,2),
  UNINSURED_AMOUNT NUMBER(18,2),
  STABILITY_FACTOR NUMBER(6,4),
  RUN_OFF_FACTOR NUMBER(6,4),
  LCR_CATEGORY STRING,
  NSFR_CATEGORY STRING,
  CONCENTRATION_FLAG STRING,
  LARGE_DEPOSIT_FLAG STRING,
  LIQUIDITY_VIOLATION_FLAG STRING,
  _SOURCE_FILE STRING,
  _LOADED_AT TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP()
);
