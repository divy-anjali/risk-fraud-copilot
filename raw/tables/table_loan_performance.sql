CREATE TABLE IF NOT EXISTS RISK_DB.RAW.LOAN_PERFORMANCE (
  PERFORMANCE_ID STRING,
  LOAN_ID STRING,
  CUSTOMER_ID STRING,
  REPORT_DATE DATE,
  DAYS_PAST_DUE NUMBER(6,0),
  DELINQUENCY_STATUS STRING,
  PAYMENT_AMOUNT_DUE NUMBER(18,2),
  PAYMENT_AMOUNT_RECEIVED NUMBER(18,2),
  OUTSTANDING_PRINCIPAL NUMBER(18,2),
  ACCRUED_INTEREST NUMBER(18,2),
  PROVISION_AMOUNT NUMBER(18,2),
  STAGE_IFRS9 STRING,
  ECL_AMOUNT NUMBER(18,2),
  RESTRUCTURED_FLAG STRING,
  WRITE_OFF_FLAG STRING,
  RECOVERY_AMOUNT NUMBER(18,2),
  RISK_MIGRATION STRING,
  _SOURCE_FILE STRING,
  _LOADED_AT TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP()
);
